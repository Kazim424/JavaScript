// Using loop to solve the problem.

// Question1:-1. Write a program that generates the multiplication table in the textbook format for a given number.

let tableof = 12;
 for(let i = 1; i<=10; i++){
    console.log(`${tableof} * ${i} = ${tableof * i}`);
 }