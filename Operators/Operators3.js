// Question-3. Write a program to find out the perimeter of a rectangle. Print the results to the console.

let length = 4;
let width = 5;

let perimeter = 2 * (length + width);
console.log(`The perimeter of rectangle with length ${length} and width ${width} is : ${perimeter}`);